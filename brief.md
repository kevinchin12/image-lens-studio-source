# 产品 Brief

## 产品定义

Image Lens Studio 不是“把图片翻译成长提示词”的工具，而是一个面向视觉创作者的可视化提示词编译器：把参考图拆成带来源的视觉模块，重新组合成可复用配方，并通过生图即时验证。

Prompt 文本是配方针对某个模型编译出的产物，不是产品的核心数据。

## 首要用户

- 高频使用参考图和生成式图像的独立设计师、AIGC 创作者与视觉导演。
- 需要维护角色、产品或品牌视觉一致性的小团队。
- 已经知道“A 图借主体、B 图借风格”，但当前依靠复制粘贴和反复改长提示词的人。

## 核心 JTBD

> 当我有 2–4 张参考图、每张只想借其中一部分时，帮我提取可迁移的视觉变量，按来源重新组合，发现冲突并快速生成验证；不必重写整条提示词，也不会无意识复刻整张原图。

## 第一条纵向闭环

1. 截图或导入两张参考图。
2. 每张图被拆成主体、风格、光线、镜头、环境、材质、构图、渲染八类模块。
3. 用户从两张图中各选至少一个模块，拖入 Recipe。
4. Compiler 按确定性规则编译提示词，展示来源、缺失项与冲突。
5. 用户生成至少一张验证图。
6. 结果作为新 Image Node 保留，并可再次拆解。
7. 项目保存后能够重新打开，配方与谱系不丢失。

如果这条链路没有完整走通，不增加社区、协作、模板市场、复杂绘图、局部重绘或大量 Provider。

## 产品差异

- **来源可见**：每个模块都能追溯到参考图和分析版本。
- **局部替换**：只替换灯光、材质或构图，不重写整条 Prompt。
- **冲突可解释**：重复或矛盾模块不会被静默糅合。
- **即时验证**：组合后直接生成，结果不是另开一次性流程。
- **配方可复用**：保存 Recipe、编译快照和生成谱系，而不仅是最终图片。

## MVP 对象

### Image Node

来源图或生成图。保存缩略图、尺寸、来源、分析状态、生成关系与候选状态。

### Prompt Module

最小可复用单位。包含稳定 ID、类别、内容、来源 Asset、分析版本、用户修订、启用状态，以及“可观察 / 推断 / 用户补充”证据标签。

不展示虚假的精确置信度；只有模型确实返回且经过校准时才使用数值置信度。

### Recipe Frame

带明确语义槽位的组合对象。同一类别允许多个模块，但必须设置主次、覆盖或冲突状态。画布距离不自动决定语义关系。

### Compiled Prompt Snapshot

Recipe 的编译结果。实时预览可以变化，发起生成时冻结为不可变快照，记录目标模型、语言、警告与用户 Override。

### Generation Run

绑定冻结快照和生成参数。结果逐张追加，不覆盖原图；支持部分成功、取消和显式重试。

## MVP 必做

- 两图导入、截图、粘贴与独立重试。
- 八类模块分析、来源标记、编辑、启用和替换。
- Recipe 槽位与确定性 Prompt Compiler。
- 重复、缺失和明显冲突提示。
- 单次生图与结果节点。
- 结果再次拆解，形成创作循环。
- 项目自动保存、重开、最小撤销/重做。
- 对象级分析和生图状态。
- API Key 迁入 Keychain。
- 明确说明图片会发送给用户选择的模型服务。

## MVP 暂不做

- 通用 Miro / tldraw 式绘图工具。
- 任意节点编程和复杂条件逻辑。
- 多人协作、云同步、社区与模板市场。
- 完整 Photoshop 式图像编辑。
- 账号、订阅和商业化后台。
- 大量 Provider 或自动“还原度”评分。
- 没有性能数据前的 Metal 渲染器。

## Alpha 验证

- 4/5 名目标用户无需指导完成两图到生图闭环。
- 中位首次闭环不超过 5 分钟。
- 至少 60% 的结果被用户判断为同时保留了两张来源图的预期变量。
- 至少一半完成任务的用户发生过一次“只替换模块、不重写整条提示词”。
- 用户能够正确说出所选模块来自哪张图。
- 主要模块人工修订率低于 30%；否则优先修分析 schema。
- 项目保存和重开无数据丢失。

这些是封闭 Alpha 的验证门槛，不是公开承诺；真实测试后再调整。
